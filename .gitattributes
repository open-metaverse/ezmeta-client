# ------------------------------------------------------------
# Unity + GitHub team dev baseline
# ------------------------------------------------------------

# Normalize line endings for text
* text=auto eol=lf

# Unity YAML assets -> use Unity Smart Merge
*.unity merge=unityyamlmerge
*.prefab merge=unityyamlmerge
*.asset merge=unityyamlmerge
*.anim merge=unityyamlmerge
*.controller merge=unityyamlmerge
*.overrideController merge=unityyamlmerge
*.mat merge=unityyamlmerge
*.physicsMaterial2D merge=unityyamlmerge
*.physicsMaterial merge=unityyamlmerge
*.playable merge=unityyamlmerge

# Treat common binary assets as binary (avoid bogus diffs)
*.fbx binary
*.obj binary
*.blend binary
*.dae binary

*.png binary
*.jpg binary
*.jpeg binary
*.tga binary
*.tif binary
*.tiff binary
*.psd binary
*.exr binary
*.hdr binary

*.wav binary
*.mp3 binary
*.ogg binary
*.flac binary
*.aiff binary
*.mp4 binary
*.mov binary
*.webm binary

# ------------------------------------------------------------
# Git LFS (optional but strongly recommended)
# If you use LFS, keep this section. If you don't, delete it.
# ------------------------------------------------------------
*.fbx filter=lfs diff=lfs merge=lfs -text
*.psd filter=lfs diff=lfs merge=lfs -text
*.exr filter=lfs diff=lfs merge=lfs -text
*.hdr filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.tga filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
